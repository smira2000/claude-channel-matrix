{
  "name": "claude-channel-matrix",
  "version": "0.1.0",
  "description": "Matrix channel plugin for Claude Code — bridge Matrix messages into Claude Code sessions",
  "license": "MIT",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun start.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "matrix-js-sdk": "^36.0.0"
  }
}
